<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Latest compiled and minified CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Latest compiled JavaScript -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
    <link rel="stylesheet" href="/stylesheets/auth.css">
    <title>
        <%=title%>
    </title>
</head>

<body>
    <div class="bg-img overlay" style="background-image: url('https://themewagon.github.io/soccer/images/bg_3.jpg');">
        <div class="container">
            <div class="row">
                <div class="alert-group">
                    <% if(success_msg!='' ){%>
                        <div class="alert alert-success alert-dismissible" role="alert">
                            <%= success_msg %>
                        </div>
                        <% } %>
                            <% if(error_msg!='' ){%>
                                <div class="alert alert-warning alert-dismissible" role="alert">
                                    <%= error_msg %>
                                </div>
                                <% } %>
                                    <% if(error!='' ){%>
                                        <div class="alert alert-danger alert-dismissible" role="alert">
                                            <%= error %>
                                        </div>
                                        <% } %>
                </div>
                <div class="col-lg-6 ml-auto">
                    <div class="choose-form">
                        <h3 class="active btn-link"><a href="/auth">Sign In</a></h3>
                        <h3 class="btn-link"><a href="/auth/register">Sign Up</a></h3>
                    </div>
                    <div class="form">
                        <form method="post" class="box">
                            <div class="mb-3">
                                <label class="label">Username</label>
                                <input type="text" class="form-control" name="username" placeholder="Username">
                            </div>
                            <div class="mb-3">
                                <label class="label">Password</label>
                                <input type="password" class="form-control" name="password"
                                    placeholder="At least 6 character">
                            </div>
                            <div class="field is-grouped">
                                <div class="control">
                                    <button class="btn btn-success">Login</button>
                                </div>
                                <div class="control">
                                    <p class="small fw-bold mt-2 pt-1 mb-0">Don't have an
                                        account? <a class="link-danger" href="/auth/register">Sign up</a>
                                    </p>
                                </div>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>

</html>